[package]
name = "etopa"
version = "0.1.0"
authors = ["Lennart Heinrich <lennart@ltheinrich.de>"]
edition = "2018"
license = "ISC"
repository = "https://github.com/ltheinrich/etopa"
description = "Time-based one-time password authenticator"
readme = "README.md"

[dependencies]
kern = "1.1.0"
lhi = "=0.0.0" # 0.0.0 is outdated
aes-gcm = "0.5.0"
rand = "0.7.3"
ring = "0.16.11"
data-encoding = "2.2.0"
